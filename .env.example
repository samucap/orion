# LlamaCloud API Key (Required for parsing PDF tables)
# Get one here: https://cloud.llamaindex.ai/
LLAMA_CLOUD_API_KEY=llx-placeholder-key

# Qdrant API Key
# This MUST match the value set in your docker-compose.yml (QDRANT__SERVICE__API_KEY)
# Here are some generated dummy keys you can use:
# Option 1: qdrant_local_dev_key_123
# Option 2: 7d9f8a2b3c4d5e6f
QDRANT_API_KEY=default_secure_key

# Qdrant URL (Local Docker instance)
QDRANT_URL=http://localhost:6333

# OpenAI API Key (Required for Embeddings)
# The system uses OpenAI to convert text into vectors (embeddings) for Qdrant.
# Without this, we cannot index or search documents.
OPENAI_API_KEY=sk-proj-placeholder-key
